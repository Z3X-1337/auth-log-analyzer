[project]
name = "auth-log-analyzer"
version = "1.0.0"
description = "Defensive SSH-style authentication log analyzer."
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
python_files = ["test_*.py"]
